{% if distro == 'buster' and apt.packages|length > 0 %}
##################################################################
# Install apt packages
##################################################################
RUN apt update \
  && apt install -y --no-install-recommends \
	{{  apt.packages|join(" \\\n\t") }} \
  && rm -rf /var/lib/apt/lists/*
{% endif %}
