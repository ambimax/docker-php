{% if distro starts with 'alpine' and apk.packages|length > 0 %}
##################################################################
# Install apk packages
##################################################################
RUN apk add --no-cache \
	{{  apk.packages|join(" \\\n\t") }}
{% endif %}
